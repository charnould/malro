// This enums file lists all available translations for:
// `widget` - corresponds to MALRO widget (except event description translations)
// `index`  - corresponds to www.malro.org start page only
// `app`    - corresponds to all page accessible when logged

//
// It must be updated when a new translation is available.
// Otherwise, this new translation won't be accessible.

export const WIDGET_TRANSLATION = ['fr', 'en'] as const
export const INDEX_TRANSLATION = ['fr', 'en'] as const
export const APP_TRANSLATION = ['fr', 'en'] as const
